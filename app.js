let tasks = [];

const addTask = ()=> {
    const taskInput = document.getElementById('taskInput')
    const text = taskInput.value
}

document.getElementById('newTask').addEventListener('clikc', function(e){
    e.preventDefault()

    addTask() 
})